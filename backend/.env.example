# ─── Server ───
PORT=8000

# ─── Database (local dev → localhost) ───
DATABASE_URL="postgresql://postgres:your_password_here@localhost:5432/VisualSearchEngine?schema=public"

# ─── JWT ───
JWT_ACCESS_SECRET="change_me_to_a_long_random_secret"
JWT_ACCESS_EXPIRES_IN="24h"

# ─── Services (local dev → localhost) ───
AI_SERVICE_URL="http://localhost:9000"
STORAGE_DIR="./storage"
